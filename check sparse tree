#include<iostream>

using namespace std;
int main()
{
    int n,count=0;
    cin>>n;
    int a[n][n];
    for(int i=0;i<n;i++)
    {
        for(int j=0;j<n;j++)
        {
            cin>>a[n][n];
        }
    }
    for(int i=0;i<n;i++)
    {
        for(int j=0;j<n;j++)
        {
            if(a[i][j]==0)
            count++;
        }
    }
    if(count>((n*n)/2))
    {
        cout<<"given matrix is sparse matrix";
    }
    else
    cout<<"not";
}
